services:
  jcink-crawler:
    build: .
    container_name: jcink-crawler
    restart: unless-stopped
    ports:
      - "8943:8000"
    volumes:
      - /opt/jcink-crawler/data:/app/data
    env_file:
      - .env
